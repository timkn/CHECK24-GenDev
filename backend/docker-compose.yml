# docker-compose.yml

version: '3.8'

services:
  web:
    build: .
    command: bash -c 'while !</dev/tcp/db/5432; do sleep 1; done; uvicorn app.main:app --host 0.0.0.0'
    environment:
       - DATABASE_URL=postgresql://postgres:hfieufh8f39f9fb3uzfg839fh3f3@144.24.175.18:5432/gendev
       - OPENAI_KEY=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
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
  db:
    image: postgres:15-alpine
    restart: always
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    ports:
      - '5432:5432'
    expose:
      - 5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_DB=gendev
      - POSTGRES_PASSWORD=jdnwbu37d8od93dhjwnb38dqdwzdh39d0dqpdfq
volumes:
  postgres_data: